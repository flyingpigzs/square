Define a class named Shape and its subclass Square.
Shape objects can be constructed by name and length has an area function which reture 0
Square subclass has an init function which take a length and name as argument and has an area method and a decribe method what prints the name of the Shape.
Print the area from Square class.
